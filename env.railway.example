# Railway Environment Variables Template
# Copy these to Railway dashboard → Service → Variables

# ============================================================================
# OPENALGO SERVICE VARIABLES
# ============================================================================

# Broker Configuration (DefinEdge)
BROKER_API_KEY=your_definedge_api_key
BROKER_API_SECRET=your_definedge_api_secret

# Security Keys (GENERATE NEW RANDOM VALUES!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
APP_KEY=generate_64_char_hex_string
API_KEY_PEPPER=generate_64_char_hex_string

# Flask Configuration
FLASK_ENV=production
FLASK_HOST_IP=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# WebSocket Configuration
WEBSOCKET_HOST=0.0.0.0
WEBSOCKET_PORT=8765

# Database (Railway will auto-provision)
DATABASE_URL=sqlite:///db/openalgo.db

# Valid Brokers
VALID_BROKERS=definedge

# Session Configuration
SESSION_EXPIRY_TIME=03:00

# ============================================================================
# TRADING AGENT SERVICE VARIABLES
# ============================================================================

# OpenAlgo Connection (use Railway internal URL)
OPENALGO_API_KEY=your_openalgo_generated_api_key
OPENALGO_HOST=http://openalgo.railway.internal:5000
OPENALGO_WS_URL=ws://openalgo.railway.internal:8765

# Trading Configuration
EXPIRY=27JAN26
ATM=24800
PAPER_TRADING=true

# State Persistence
STATE_DB_PATH=/app/state/live_state.db

# Telegram Notifications (Optional)
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Railway Internal URLs:
#    - Services communicate via internal URLs
#    - Format: http://<service-name>.railway.internal:<port>
#
# 2. Public URLs:
#    - OpenAlgo needs public access for broker callback
#    - Railway provides: https://<service>-<project>.up.railway.app
#
# 3. Volumes:
#    - OpenAlgo: Mount /app/db for database persistence
#    - Trading Agent: Mount /app/state for state persistence
#
# 4. Security:
#    - Generate NEW random keys for production
#    - Never commit actual API keys
#    - Use Railway's secret management
